let mensagens: string = "Hello Typescript";
mensagens = "Olá Typescript";

let numeros: number = 0;

console.log(mensagens)

function somar1 (a: number, b: number): number {
    return a+b ;
}
